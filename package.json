{
  "name": "weather-command-center",
  "version": "0.2.1",
  "private": true,
  "description": "Home Assistant Weather Command Center custom card",
  "scripts": {
    "build": "node scripts/build.mjs"
  }
}
